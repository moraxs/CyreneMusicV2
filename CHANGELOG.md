# 更新日志

本文件记录 Cyrene Music Reborn 各版本的变更。

## 2.0.3

### 新增
- **iOS 后台播放 + 锁屏/控制中心 Now Playing 媒体卡片**：切到后台或锁屏仍持续播放，支持系统媒体控制。
- **iOS 自签名分发更新引导**：检测到新版本后，引导用户用 AltStore / Sideloadly 重新签名覆盖安装（iOS 不允许应用内自动更新）。
- **搜索分类 Tab「播放全部」**：一键把当前分类整组歌曲加入播放序列并立即播放。

### 修复
- **聚合搜索音源逐平台惰性回退**：原来一点击会把网易云、酷狗、QQ、酷我、Apple 全部请求一遍（且可能播到非目标平台）；现在默认优先播放网易云，解析或加载失败才按 酷狗→QQ→酷我→Apple 依次回退，只请求到第一个真正能播放的平台为止。
- 同一首歌优先播放首个**真正可加载**的平台，不再出现"酷狗可播却最终播放酷我"的情况。

### 其他
- 版本号升至 **2.0.3**。
- 后续发版请将版本号同步更新到 `pubspec.yaml` 与 `lib/app/app_version.dart`。
